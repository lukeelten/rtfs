#include <iostream>
#include <bitset>

#include "rtfs.h"

using namespace std;

int main() {

    cout << "Addr: " << sizeof(InodeAddress) << endl;
    cout << "Inode: " << sizeof(Inode) << endl;

    return 0;
}